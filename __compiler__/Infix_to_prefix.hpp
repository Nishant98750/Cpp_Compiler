#include<iostream>
#include<vector>
#include "Tokenizer.hpp"

using namespace std;
class prefix_converter{
      vector<TokenType>tokens;

      public:
      prefix_converter(vector<TokenType>tk):tokens(move(tk)){
      }
      vector<TokenType> convert(){
            for(auto i=tokens.begin();i!=tokens.end();i++){
                  if (){
                        
                  }
            }
      }
};